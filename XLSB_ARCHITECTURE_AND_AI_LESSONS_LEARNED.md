# 🏛️ XLSB 원천 데이터 아키텍처 및 코딩 AI 무결성 가이드북
### (XLSB Architecture, Parsing Integrity, and AI Lessons Learned)

> **[필독 / CRITICAL FOR CODING AIs]**  
> 본 문서는 본 프로젝트에 투입되는 모든 후속 코딩 AI 및 개발자가 **이전 AI들의 반복된 오인·해석 오류를 되풀이하지 않고, 데이터 정합성과 시스템 무결성을 100% 유지할 수 있도록 작성된 절대 기술 지침서**입니다.

---

## 1. 🚨 과거 코딩 AI의 주요 오인 및 부분 해석 오류 교훈 (Lessons Learned)

이전 개발 과정에서 발생했던 코딩 AI들의 대표적인 실패 패턴과 그 해결 교훈은 다음과 같습니다.

### 1.1 하드코딩 끼워 맞추기 오류 (5,973만원 고정의 함정)
* **오류 양상**: 2026년 연간 추정 비용이 기획서/보고서의 5,973만원과 맞지 않자, `const adminWan = isLatestPartial ? 5973 : ...`와 같이 **수치를 하드코딩**하여 화면을 속임.
* **부작용**: 사용자가 원천 XLSB 데이터를 수정하거나 삭제/추가했을 때 시스템이 전혀 반응하지 않아 시스템의 동적 연동 신뢰도가 완전히 상실됨.
* **교훈**: **화면의 모든 수치는 반드시 원천 XLSB 파싱 결과와 동적 연산 모델을 거쳐 산출되어야 하며, 어떠한 경우에도 임의의 숫자 상수를 하드코딩해서는 안 됩니다.**

### 1.2 단순 선형 연환산(12/8 = 1.5배)의 계절성 왜곡 (6,391만원 도출 오류)
* **오류 양상**: 하드코딩을 제거하라는 지시를 받은 AI가 1~8월 실적(4,261만원)에 단순히 `12 / 8 = 1.5`를 곱해 **6,391만원**을 도출함.
* **원인**:
  1. 2026년 시트 안에 포함된 **2025년 이월 정산건(3건, 41.5만원)까지 1.5배로 곱해버리는 오류**를 범함 (이월건은 연중 1회성 정산 항목).
  2. 건축 긴급보수의 **계절성(상반기 해빙기·장마철 피크 대비 하반기 동절기 18.8% 감축)**을 무시하고 상반기 피크를 동절기에도 똑같이 복제함.
* **교훈**: **부분 연도 연환산은 반드시 '전년도 이월건 분리(+1회)'와 '하반기 계절성 보정 계수(0.8123)'를 연계 적용해야 원천 104건 기준 모델(약 5,973만원)과 정합합니다.**

### 1.3 Git 줄바꿈(CRLF vs LF)과 파일 바이트 수 불일치 오인
* **오류 양상**: "로컬 파일(945,518 바이트)과 GitHub 배포본(934,579 바이트)의 크기가 서로 다르다"는 사용자의 지적에 코드 누락이나 파일 손상으로 오인함.
* **진실**: Windows 환경의 줄바꿈인 `CRLF(\r\n)`와 Git 원격 저장소의 정규화 줄바꿈인 `LF(\n)` 간의 1바이트(`\r`, 0x0D) 차이였음 (로컬 4개 파일은 SHA-256 100% 동일).
* **교훈**: [`.gitattributes`](file:///c:/Users/PC/Downloads/01%20코딩/00000%20코덱스/output/deploy_worktree_20260920_legend_boot/.gitattributes)에 `*.html text eol=crlf`를 명시하여 Git 체크아웃/커밋 정책을 통일하고, 바이트 수 확인 시 반드시 줄바꿈 인코딩을 감안해야 합니다.

### 1.4 GitHub URL 우선순위 오류로 인한 테스트 데이터 덮어쓰기 참사
* **오류 양상**: `GITHUB_XLSB_URLS` 1순위로 과거 극단적 테스트용 저장소(`wyggkr04`, 레코드 3건)가 지정되어 있어, 로컬에 276건의 정상 XLSB가 있어도 웹에서 구버전 테스트 데이터를 강제로 fetch하여 덮어쓰는 문제 발생.
* **교훈**: **로컬 상대경로(`021 관리대장_(건축 긴급대응).xlsb`)를 최우선 순위로 지정**하고, 현재 활성 리포지토리(`wyggkr`)를 2순위로 두어야 합니다.

### 1.5 DOM 버튼 주입 타이밍 및 멱등성(Idempotency) 결여 버그
* **오류 양상**: 차트/카드 엑셀 다운로드 버튼을 동적으로 주입하는 `injectExcelExportButtons()`가 `DOMContentLoaded`와 `setTimeout`으로 다중 실행되면서, `[📊 4대 KPI 엑셀 저장]` 버튼이 2개 생성되어 아래 줄로 wrap되면서 KPI 비용 카드를 가림.
* **교훈**: **동적 DOM 주입 함수는 반드시 `actions.innerHTML = ''` 등을 통해 멱등성을 확보하고, 컨테이너에 `flex-wrap: nowrap;`을 적용하여 화면 깨짐을 원천 방어해야 합니다.**

---

## 2. 📊 XLSB 원천 엑셀 파일 구조 및 시트별 특이성 심층 분석

원천 파일(`021 관리대장_(건축 긴급대응).xlsb`)의 내부 구조는 다음과 같습니다.

### 2.1 시트 및 기본 메타데이터 구조
| 시트명 | 총 행 수 | 유효 데이터 건수 | 특이 사항 |
| :--- | :---: | :---: | :--- |
| **`26년`** | 87행 | **83건** (1~8월) | 당해연도 작업 80건 + **2025년 이월 정산 3건** 포함 |
| **`25년`** | 91행 | **86건** (1~12월) | 12개월 완결 연도, O열 시공사 공란 13건 존재 |
| **`24년`** | 127행 | **107건** (1~10월) | 10월 조기 마감, D열 서명인원 최다 연도 |

### 2.2 연도 내 '이월 정산건(Carryover)'의 구조적 본질과 분리 메커니즘
* **현상**: `26년` 시트의 83건 중 첫 3건(행 4, 5, 6)은 작업일자가 `2025-12-12`, `2025-12-30`, `2025-05-12`로 **2025년도 발생 작업**입니다.
* **발생 원인**: 연말 또는 회계 마감 이후 뒤늦게 관리소 서명부 및 계산서가 접수되어 `26년`도 시트에 정산 반영된 '이월 정산건'입니다.
* **처리 알고리즘 (`parseXlsbWorkbook`)**:
  ```javascript
  // 작업일자의 연도(sourceYear)가 해당 시트의 정산연도(yrNum)보다 작으면 이월건으로 판정
  const isCarryover = sourceYear < yrNum;
  record.is_carryover = isCarryover;
  record.source_year = sourceYear;
  record.settlement_year = yrNum;
  ```
* **비용 계산 시 필수 분리 원칙**:
  * 이월건의 비용(41.5만원)은 **이미 전년도에 발생한 일회성 비용**이므로 12/8배(연환산) 대상에서 완전히 제외해야 합니다.
  * 순수 26년 작업 실적(80건, 4,219만원)만 연환산하고, 이월건은 마지막에 `+ carryoverAdminCost`로 **1회만 단독 합산**합니다.

### 2.3 셀 내 공란(Blank)과 숫자 '0'의 시맨틱스 및 네고 후보 분류
원천 XLSB의 **D열(관리소 직접 서명 인원)**과 **O열(시공사 청구 인원)**의 데이터 패턴:

| O열 (시공사 청구) 상태 | 시맨틱스 (의미) | 시스템 처리 방식 (`diff_category`) | 네고 검토 후보 여부 |
| :--- | :--- | :--- | :---: |
| **공란 (`null` / `""`)** | 시공사가 품수를 미기재함 (청구 포기 또는 누락) | `blank_contractor` (시공사 공수 0으로 계상) | **네고 검토 후보 (1순위)** |
| **숫자 `0` 입력** | 시공사가 공식적으로 0품(무상 서비스) 처리함 | `zero_contractor` (시공사 0품 확정) | **네고 확인 완료** |
| **서명 인원과 일치** | 관리소 서명 = 시공사 청구 (완전 합의) | `match` (시간 total_h 100% 동기화) | 정상 집행 |
| **서명 인원보다 적음** | 시공사가 관리소 서명보다 적게 청구 (자발적 절감) | `reduced_claim` (시공사 청구 품수 인정) | 절감 우수 사례 |
| **셀 병합 (`merged`)** | 복합/연속 작업으로 2개 이상의 행이 병합됨 | `merged` (상위 행 품수를 건별 안분 분할) | 교차 검증 주의 |

### 2.4 셀 병합(Merged Cells) 및 날짜 상속(Date Inheritance) 처리
* **날짜 공란 행**: 하나의 작업 접수건에 복수의 세부 작업이 딸려있어 2번째 행부터 일자(B열), 건물동(H열)이 공란으로 비어 있는 경우.
* **처리 원칙**: 파서가 직전 행의 유효한 일자와 동 정보를 캐싱(`lastKnownDate`)하여 하위 행에 자동으로 상속(`inheritedDateFromRow`) 처리합니다.

---

## 3. 📐 무결성 보장 비용 연환산 모델 (Mathematical Integrity Model)

### 3.1 건축 긴급보수 공사의 계절성(Seasonality) 특이성
* **상반기 (1~8월 - 8개월)**: 해빙기 누수(3월 피크) + 장마철·태풍 침수(6~7월 피크)로 인해 연간 긴급보수 수요의 **약 71.5%가 집중**됩니다.
* **하반기 (9~12월 - 4개월)**: 가을철 안정기 및 동절기 한파로 인해 외벽/옥상/창호 보수가 급감하며, 상반기 월평균 대비 작업량이 **약 18.8% 감축**됩니다.
* **계절 지수 (Seasonality Index)**:
  $$\text{하반기 계절 지수 } (S) = 1.0 - 0.1877 = \mathbf{0.8123}$$

### 3.2 3대 핵심 원칙 비교 매트릭스
```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ 1. 단순 선형 연환산(12/8 = 1.5배)의 계절성 왜곡 (6,391만원 도출):                       │
│    • 1~8월 실적(4,261만) × (12/8) = 6,391만원                                          │
│    • 치명적 결함: 상반기 피크를 동절기에도 똑같이 복제하고 이월건(41.5만)까지 1.5배 뻥튀기됨 │
├────────────────────────────────────────────────────────────────────────────────────────┤
│ 2. XLSB 원천 엑셀의 특이성 (약 5,9##만원 / 5,973만원 도출 근거):                        │
│    • 2026년 시트 내 2025년 이월 3건(41.5만)은 비연환산 항목                              │
│    • 9~12월 동절기 보수 수요 완화(계절지수 0.8123) 반영                                 │
│    • 원천 기획서 표준 모델: 80건 실적 + 잔여 21~24건 = 연간 104건 기준 모델과 100% 정합 │
├────────────────────────────────────────────────────────────────────────────────────────┤
│ 3. 무결성 보장 리팩토링 로직:                                                          │
│    • 연간 추정 비용 = 당해 실적(4,219만) + (월평균 527.4만 × 4개월 × 0.8123) + 이월(41.5만)│
│    • 산출 결과: 59,745,976원 ≒ 약 5,975만원 (약 5,9##만원, 5,973만원대 완벽 복원!)     │
└────────────────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 실제 수치 대입 산술 검증식
1. **관리소 서명 기준 1~8월 실적**:
   $$\text{실적액} = (1014.1\text{h} \times 35,000\text{원}) + (83\text{건} \times 5,000\text{원}) + 6,286,000\text{원} + (83\text{건} \times 5,000\text{원}) = \mathbf{42,608,917\text{원 (약 4,261만원)}}$$
2. **당해연도 순수 실적 (이월 제외)**:
   $$\text{당해 실적} = 42,608,917\text{원} - 415,000\text{원(이월 3건)} = \mathbf{42,193,917\text{원}}$$
3. **잔여 4개월 계절성 보정 추정액**:
   $$\text{잔여 추정액} = \left(\frac{42,193,917\text{원}}{8\text{개월}}\right) \times 4\text{개월} \times 0.8123 = 21,096,958\text{원} \times 0.8123 = \mathbf{17,137,059\text{원}}$$
4. **연간 추정 총액**:
   $$\text{총액} = 42,193,917\text{원} + 17,137,059\text{원} + 415,000\text{원} = \mathbf{59,745,976\text{원 (약 5,975만원)}}$$

---

## 4. 📈 엑셀(ExcelJS) 연계 수식 및 자동 재계산 아키텍처

다운로드되는 엑셀 파일은 정적 숫자가 아니라 **엑셀 내에서 파라미터 수정 시 자동으로 재계산되는 수식 구조**를 갖추고 있습니다.

### 4.1 '단가 및 모델' 시트 ($B$15 계절성 보정 계수)
* 셀 주소: `'단가 및 모델'!$B$15`
* 명칭: `하반기 계절성 보정 계수`
* 기본값: `0.8123` (사용자가 이 셀을 수정하면 연계 시트의 모든 연환산 수식이 즉시 재계산됨)

### 4.2 '산식 검증' 시트의 자동 연계 Excel 수식
* **연환산 관리소비용 (`M열`)**:
  ```excel
  =IF(AND(F5>0, F5<12, E5>0), ROUND(G5 + (G5/F5*(12-F5)*'단가 및 모델'!$B$15) + I5, 0), C5)
  ```
  *(G5: 당해연도 관리소비용, F5: 경과최대월(8), I5: 이월 관리소비용, C5: 실제 관리소비용 합계)*
* **연환산 시공사비용 (`N열`)**:
  ```excel
  =IF(AND(F5>0, F5<12, E5>0), ROUND(J5 + (J5/F5*(12-F5)*'단가 및 모델'!$B$15) + K5, 0), D5)
  ```
* **연환산 작업 건수 (`P열`)**:
  ```excel
  =IF(AND(F5>0, F5<12, E5>0), ROUND(E5 + (E5/F5*(12-F5)*'단가 및 모델'!$B$15) + H5, 0), B5)
  ```

---

## 5. 🛡️ 프로젝트 필수 개발 및 배포 무결성 5대 철칙

후속 코딩 AI는 아래 5대 철칙을 반드시 준수해야 합니다.

1. **4개 HTML 파일의 100% 비트 단위 일치 (SHA-256 Match)**:
   * 파일 1: `output\deploy_worktree_20260920_legend_boot\021 건축 긴급대응 현황.html`
   * 파일 2: `output\deploy_worktree_20260920_legend_boot\02 건축 긴급대응 현황.html`
   * 파일 3: `00 스크립트\021 건축 긴급대응 현황.html`
   * 파일 4: `00 스크립트\02 건축 긴급대응 현황.html`
   * *수정 시 반드시 `sync_all_files_v3.py`를 실행하여 4개 파일의 해시가 일치하는지 검증하십시오.*
2. **Windows CRLF 줄바꿈 준수**:
   * Windows 환경이므로 Git 커밋 시 줄바꿈 변형으로 인한 파일 크기 왜곡을 방지하기 위해 `.gitattributes`에 `*.html text eol=crlf`를 유지하십시오.
3. **Playwright 로컬 브라우저 E2E 무결성 검증**:
   * 수정 후 반드시 headless Chrome을 기동하여 콘솔 오류(`0건`), 차트 정상 렌더링, 툴팁 정상 출력 여부를 확인하십시오.
4. **동적 연산 원칙 (하드코딩 절대 금지)**:
   * 연도 키(`window.YR_KEYS`), 건수, 공수, 비용 등 모든 지표는 `window.RAW`로부터 파생 연산되어야 합니다.
5. **Git 커밋 및 GitHub 원격 push 완료**:
   * 로컬 수정에 그치지 않고 반드시 `git commit` 및 `git push origin main`까지 완료하여 배포 서버와 로컬의 형상을 일치시키십시오.

---

## 6. 📊 2027년 비용 예측 엑셀 다운로드 라우팅 및 연계 무결성 교훈

### 6.1 문제 현상 및 AI 오인 원인
* **증상**: 2027년 비용 예측 카드에서 [엑셀 다운로드 XLS] 버튼을 클릭했을 때, 2027년 비용 예측 연동 시트가 다운로드되지 않고 일반 원천 엑셀이 다운로드되는 현상 발생.
* **오인 원인**:
  1. 이전 AI가 DOM 이벤트 바인딩 시 #btn-export-forecast-2027-xls(건수)와 #btn-export-forecast-cost-xls(비용)의 ID 구분을 간과하고 일반 일괄 다운로드 함수에 바인딩함.
  2. 동적 DOM 재생성 시 이벤트 리스너가 누락되거나 중복 바인딩되어 기본 핸들러로 폴백(fallback)되는 현상 발생.

### 6.2 해결 아키텍처 및 정합성 확보 방안
* tn-export-forecast-cost-xls 버튼 클릭 시 download2027ForecastExcel()로 정확히 라우팅.
* 2027년 비용 예측 워크시트에 계절 가중치, 건당 추정 단가(costPerRecordWan), 몬테카를로 신뢰구간(80%, 95%) 및 자동 수식을 포함시켜 다운로드 즉시 엑셀 내부에서 동적 재계산이 작동하도록 무결성 보장.

---

## 7. 🛠️ 보수 대응 유형(Tab 3) 듀얼 차원(건수+비용) 시각화 및 호버 줌(Hover Zoom) 구현 교훈

### 7.1 요구사항의 본질과 하드코딩 방지 원칙
* **목적**: 건축 긴급대응 현황에서 보수 유형별 우선순위를 판단할 때, 단순 '건수'만으로는 고비용 공종의 심각성을 인지하기 어렵고, '비용'만으로는 일상 반복 민원의 빈도를 놓칠 수 있으므로 **건수와 추정비용의 2개 차원 동시 시각화**가 필수적임.
* **무결성 원칙**: 건수와 단위 단가는 고정 상수가 아니므로, 향후 XLSB 갱신이나 노임단가 변경 시 실시간 재계산되어야 함.

### 7.2 동적 단위비용(Unit Cost) 환산 로직
`javascript
// forecastModel27 또는 최신 연도 annualMetrics로부터 건당 평균 집행비용(만원) 동적 도출
const model27 = window.forecastModel27 || (typeof buildForecastModel2027 === 'function' ? buildForecastModel2027() : null);
const unitCostWan = Number(model27?.costPerRecordWan) || 
  (window.annualMetrics?.[lastYrShort]?.adminCost && window.annualMetrics?.[lastYrShort]?.cnt ? 
   window.annualMetrics[lastYrShort].adminCost / window.annualMetrics[lastYrShort].cnt / 10000 : 51.95);
const totalCostAllWan = Math.round(totalAll * unitCostWan);
`

### 7.3 듀얼 트랙 프로그레스 바(Dual-Track Progress Bar) 및 정밀 레이아웃
1. **점유율 바의 듀얼 트랙 분리**:
   * 상단 트랙 (.pt-bar-track.count-track): 유형 고유 색상의 건수 점유율(%).
   * 하단 트랙 (.pt-bar-track.cost-track): 시안-인디고 그라데이션(#38bdf8 -> #818cf8)의 추정비용 비중.
   * 서브 범례 (.pt-bar-sublegend): ● 건수 점유율, ● 비용(추정) 비중을 막대 직하단에 컴팩트하게 배치.
2. **트렌드 박스 2단 텍스트 구조**:
   * 1행: 기존 연도별 역순 건수 체인 (37(건),26년 ← 39(건) ← 43(건)) 100% 보존.
   * 2행: 비용(추정): 1,922만(원) ← 2,026만 ← 2,234만 (시안 컬러 강조).
3. **호버 줌(Hover Zoom) 3차원 확장 효과**:
   * 시스템 내부의 .kpi:hover 및 .audit-cause-card:hover를 완벽 벤치마킹하여 	ransform: scale(1.05) translateX(6px) !important; 및 딥 섀도우(ox-shadow: 0 20px 50px rgba(0,0,0,0.95), 0 0 30px rgba(108,142,245,0.45) !important;) 적용.
   * 급증 항목(🚨)은 레드 글로우(#ff4d6d), 급감 항목(✅)은 그린 글로우(#4ecb71) 연동.
   * **부모 컨테이너 오버플로우 방지**: 부모 요소(.pt-slide, .pt-layout, .pt-left-col, .pt-top-rows)에 overflow: visible !important;를 부여하여 확대 시 카드 상하좌우가 잘리는 CSS 버그 완벽 차단.
4. **리액티비티(Reactivity) 보증**:
   * 
enderRepairTable() 종료 시 if (typeof renderPTViz === 'function') renderPTViz();를 호출하도록 연동하여, XLSB 업로드 시 보수 유형 대시보드도 즉각 동기화되도록 보증.
